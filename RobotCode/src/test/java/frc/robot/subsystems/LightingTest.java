/*package frc.robot.subsystems;

import org.junit.Test;
import frc.robot.subsystems.Lights;
import static org.junit.Assert.assertEquals;

public class LightingTest {
    @Test
    public void conversionTest() {
        assertEquals(Lights.getInstance().interpretColor('R'), 0);
        assertEquals(Lights.getInstance().interpretColor('G'), 120);
        assertEquals(Lights.getInstance().interpretColor('B'), 180);
        assertEquals(Lights.getInstance().interpretColor('Y'), 60);
        assertEquals(Lights.getInstance().interpretColor('f'), 300);
    }

}*/